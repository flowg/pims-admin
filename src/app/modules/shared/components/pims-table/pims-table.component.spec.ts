/*
import {
    ComponentFixture,
    TestBed
} from '@angular/core/testing';

import { PimsTableComponent } from './pims-table.component';

describe( 'PimsTableComponent', () => {
    let component: PimsTableComponent;
    let fixture: ComponentFixture<PimsTableComponent>;

    beforeEach( async () => {
        await TestBed.configureTestingModule( {
                declarations: [ PimsTableComponent ]
            } )
            .compileComponents();
    } );

    beforeEach( () => {
        fixture = TestBed.createComponent( PimsTableComponent );
        component = fixture.componentInstance;
        fixture.detectChanges();
    } );

    it( 'should create', () => {
        expect( component ).toBeTruthy();
    } );
} );
*/
